{
  "name": "decorator",
  "version": "1.0.0",
  "description": "Decorator attaches additional responsibilities to an object dynamically. Decorator provide a flexible alternative to a class for extending functionality.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/node": "^18.0.0"
  }
}
